# Fixtures

Representative query sets and deterministic test fixtures belong here.

- `representative-queries.json`: initial EPIC-03 optimizer eval set. The eval
  script accepts any JSON array of strings, so this file can be expanded toward
  the planned 200+ production gating queries without changing code.
